#include <stdio.h>

#include "version.h"

int main(int argc, char **argv) {
        printf("this is going to be something cool someday!\n");
        printf("running version %s\n", VERSION);

        return 0;
}